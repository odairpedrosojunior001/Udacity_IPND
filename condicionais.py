def bigger (x,y): # codigo para trazer o maior numero entre 2 numeros (x , y ).
    if x>=y:
        return x
    return y
print bigger(4,5)
def bigger (x,y): # 2 maneira de fazer codigo para trazer o maior numero entre 2 numeros (x , y ).
    if x>=y:
        return x
    return y
    if x>=y:
        return x
    else:
        return y
print bigger(6,5)
def is_friend("amigo"): #função que pega todos os nomes de amigos com D e printa true, o restante , false.
    if "amigo"[0]="D":
        return "true"
    else:
        return "false"
print def is_friend("Daniel")
